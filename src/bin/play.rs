use rudoku::board::Board;

// cargo run --bin play -- create
// cargo run --bin play -- open samples/sample1.txt

fn main() {
    // let board = Board::from_file("test_samples/test_valid_1.txt").unwrap();
    // board.to_file("hi.txt").unwrap();

    let mut x: u8 = 0;
    x = x - 1;
    println!("{}", x);
}
