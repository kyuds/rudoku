use crate::board::Board;

/// Use backtracking algorithm to solve the sudoku puzzle.
/// Sudoku might be unsolvable --> hence return false
pub fn backtrack_solver(board: &mut Board) -> bool {
    true
}

// Using a genetic algorithm to solve the sudoku puzzle.
// Genetic algorithms are probabilistic, thus we may not
// be able to solve a solvable board. 
// pub fn genetic_solver(board: &mut Board) -> bool {
//     true
// }
